name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Test
        run: pytest
      - name: Code quality
        run: ruff check src tests
      - name: Compile
        run: python -m compileall -q src tests
      - name: Validate example
        run: paperci validate examples/minimal-project.yaml
      - name: Lint intentional overclaim example
        run: paperci lint examples/minimal-project.yaml --fail-on never
      - name: Exercise one-command demo
        run: |
          paperci demo ci-demo
          paperci doctor ci-demo
          paperci lint ci-demo --fail-on never

  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Build wheel and source archive
        run: |
          python -m pip install build
          python -m build
      - name: Smoke-test built wheel
        run: |
          python -m pip install --force-reinstall dist/*.whl
          paperci demo wheel-demo
          paperci doctor wheel-demo
      - uses: actions/upload-artifact@v6
        with:
          name: paperci-distributions
          path: dist/*
